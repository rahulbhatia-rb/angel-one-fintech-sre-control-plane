# Terraform Direction

Use reusable, versioned modules with:

- remote state and locking
- policy checks in CI
- environment isolation
- drift detection
- imports/recovery procedures
- clear ownership boundaries

Potential module families: VPC, EKS, IAM, observability integration, delivery primitives, and resilience defaults.

The goal is to encode safe defaults while keeping application-team inputs narrow and intentional.
